<div class="mt-8 max-w-2xl mx-auto">
  <h2 class="text-lg font-bold text-center mb-4 text-slate-700 dark:text-slate-100">タスク一覧</h2>
  <div class="bg-white/80 dark:bg-slate-800/90 backdrop-blur-sm rounded-2xl shadow-lg dark:shadow-slate-900/50 border border-slate-200/50 dark:border-slate-700/50 overflow-hidden">
    <div class="overflow-x-auto max-h-64 overflow-y-auto">
      <table class="w-full text-xs">
        <thead class="sticky top-0">
          <tr class="bg-slate-100 dark:bg-slate-700/80">
            <th class="px-3 py-2 text-left font-bold text-slate-700 dark:text-slate-200">Name</th>
            <th class="px-3 py-2 text-left font-bold text-slate-700 dark:text-slate-200">Assignees</th>
            <th class="px-3 py-2 text-left font-bold text-slate-700 dark:text-slate-200">Status</th>
            <th class="px-3 py-2 text-left font-bold text-slate-700 dark:text-slate-200">Created</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200 dark:divide-slate-700">
          <% if tasks.present? %>
          <% tasks.each do |task| %>
          <tr class="hover:bg-slate-50 dark:hover:bg-slate-700/30 transition-colors">
            <td class="px-3 py-2 text-slate-600 dark:text-slate-300 max-w-[200px] truncate"><%= task["name"] %></td>
            <td class="px-3 py-2">
              <% if task["assignees"].present? %>
              <% task["assignees"].each do |assignee| %>
              <div class="flex items-center space-x-1">
                <img src="<%= assignee["profilePicture"] %>" alt="<%= assignee["username"] %>" class="w-5 h-5 rounded-full">
                <span class="text-slate-600 dark:text-slate-300 truncate max-w-[80px]"><%= assignee["username"] %></span>
              </div>
              <% end %>
              <% else %>
              <span class="text-slate-400 dark:text-slate-500">未設定</span>
              <% end %>
            </td>
            <td class="px-3 py-2">
              <span class="px-2 py-0.5 rounded-full text-xs font-medium
                <% case task["status"]["status"] %>
                <% when "完了" %>
                bg-emerald-100 text-emerald-700 dark:bg-emerald-900/50 dark:text-emerald-400
                <% when "進行中" %>
                bg-blue-100 text-blue-700 dark:bg-blue-900/50 dark:text-blue-400
                <% when "未着手" %>
                bg-slate-100 text-slate-600 dark:bg-slate-700 dark:text-slate-400
                <% else %>
                bg-amber-100 text-amber-700 dark:bg-amber-900/50 dark:text-amber-400
                <% end %>
              "><%= task["status"]["status"] %></span>
            </td>
            <td class="px-3 py-2 text-slate-500 dark:text-slate-400">
              <% if task["date_created"].present? %>
              <%= Time.at(task["date_created"].to_i / 1000).strftime('%m/%d') %>
              <% else %>
              --
              <% end %>
            </td>
          </tr>
          <% end %>
          <% else %>
          <tr>
            <td colspan="4" class="px-3 py-4 text-center text-slate-500 dark:text-slate-400">タスクが見つかりません。</td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
